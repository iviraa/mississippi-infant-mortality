# Copy to .env and fill in values

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=ms_health
PGUSER=postgres
PGPASSWORD=postgres

# Census API key (free signup at https://api.census.gov/data/key_signup.html)
CENSUS_API_KEY=your_key_here
